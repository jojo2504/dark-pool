# ─── Dark Pool — Environment Variables ────────────────────────────────────────
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to version control.

# ── Database ───────────────────────────────────────────────────────────────────
# PostgreSQL connection string (local dev: docker run -p 5432:5432 postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/darkpool?schema=public"

# ── Sumsub KYB ─────────────────────────────────────────────────────────────────
# Get from: https://cockpit.sumsub.com → Developer → App tokens
SUMSUB_APP_TOKEN="sbx:YOUR_APP_TOKEN_HERE"
SUMSUB_SECRET_KEY="YOUR_SUMSUB_SECRET_KEY"

# Webhook secret for signature verification (set in Sumsub dashboard)
SUMSUB_WEBHOOK_SECRET="YOUR_WEBHOOK_SECRET"

# ── ComplyAdvantage Sanctions Screening ────────────────────────────────────────
# Get from: https://app.complyadvantage.com → Settings → API
COMPLY_ADVANTAGE_API_KEY="YOUR_COMPLY_ADVANTAGE_API_KEY"

# ── On-chain Admin Credentials ─────────────────────────────────────────────────
# Private key of the platform admin wallet (holds ADMIN_ROLE on ShadowBidFactory)
# Used server-side only to call verifyInstitution / revokeInstitution
# Generate a dedicated wallet — DO NOT use personal funds wallet
PLATFORM_ADMIN_PRIVATE_KEY="0xYOUR_ADMIN_PRIVATE_KEY_HERE"

# Deployed ShadowBidFactory contract address
FACTORY_CONTRACT_ADDRESS="0xYOUR_FACTORY_ADDRESS"

# Also expose factory address to the browser
NEXT_PUBLIC_FACTORY_ADDRESS="0xYOUR_FACTORY_ADDRESS"

# ── Next.js ────────────────────────────────────────────────────────────────────
NEXTAUTH_SECRET="YOUR_NEXTAUTH_SECRET_FOR_CSRF"
NEXTAUTH_URL="http://localhost:3000"

# ── Cron Protection ────────────────────────────────────────────────────────────
# Secret header value for /api/cron/rescreen (set same value in Vercel Cron config)
CRON_SECRET="YOUR_RANDOM_CRON_SECRET"

# ── Optional: Block Explorer API keys ─────────────────────────────────────────
ETHERSCAN_API_KEY=""
